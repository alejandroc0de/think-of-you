# Install react Router dom 
# Tree is src and then pages for the auth pages and app for the home page
# Once client is connected we read the token saved when client logged on. Then i create the socket 
# I use navigate cause i cannot render a route and a router in the return of a function, so import Usenavigate and instance it, then use it passing the router where i want to navigate to

# Cors is blocking fetch request from the frontend, so i added cors to the server, and specify the route that will be allowed, it can be found un ENV so i can update it later once deployed, THere are 2 cors, one for socket and another for express routes  
# From react home i call io to pass the token so i need cors for socket io as well 

# Everything outside the component is called when imported so the sending of the token has to happen once the component is mounted, hence inside the component using a useEffect 

# I created a ENV for the client, in react has to start with VITE_ and the use variables for the code fetchs

if(!result.ok) checks if the answers from the back is in the 200 range 
When username is already in postgress, postgres returns error 23505 so i save it in the json of the res and validate in the front to show descriptive error

# If i do a fetch within a useEffect first i declare the function and at then end of useEffect i call the function 
useEffect solo puede retornar una clean up o nada, hacer fetch y await dentro del effect pues devuelve una promesa

# To update the map i returnt the obj entered by the back to the front, then add it to the array of recentmessages, which then re renders the map 
# So ai has suggested to emit message to myself also and not only the sender, cause i have 2 sources of truth, and then use the http to just render ui
sounds ok, but i will give it some more thought, the main idea is to emit to both me and the sender when i send message so it gets saves to the db also.

# The message is added to the top of the array because of the setup of the spread 

# So i will add a empty div to the bottom of the messages div, and everytime messages changes scroll to that div so last message will always show. Ref es como doc.getelementById

# the token has username and id, with that info we can use the username, so install jwt-decode and decode 
# Usestate is mainly for values that change, my username and myid are const and wont change so const within the component is ok 

## PROTECTED ROUTE, so i created a new folder Component, it has the protected route of home, i call it before rendering home as a route, it acts as a middleware, calls the function 
envolving and within that function if there is not a token i call login, otherwise i return the children which is the same home

# When using a useEffect with a function inside, dont forget to call the function within the useEffect

# For the partner, i use conditional rendering, so on mounting i call the back and check if client has partner, if he does, process continues normally, and if client has no partner 
i use conditional rendering